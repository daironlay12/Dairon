/**
 * Scene 4 — the interior. A dark marble/glass panel that irises open
 * (clip-path) from the center of the frame, as if the camera has just
 * crossed the building's threshold. It ends fully opaque and becomes the
 * backdrop the PrestigeCard scene appears against.
 */
export function BuildingApproach() {
  return <div className="stage-layer layer-building" />;
}
